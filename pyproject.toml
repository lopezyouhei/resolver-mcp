[project]
name = "resolver-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp==3.4.2",
    "langchain>=1.3.10",
    "langchain-mcp-adapters>=0.3.0",
    "langgraph>=1.2.6",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
